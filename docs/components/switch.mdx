---
slug: switch
title: Switch
---

import React from 'react'
import { Switch } from 'rewind-uikit'
import { DocsTemplate } from '../../src/components/DocsTemplate.tsx'

export const SwitchPreview = () => {
  return <Switch id='mode' color='primary' />
}

Switch components toggle between on and off states, making them ideal for settings and preferences.
They offer a more intuitive alternative to checkboxes for binary choices.

<div className="flex flex-col gap-8 justify-center">
  <DocsTemplate preview={<SwitchPreview />}>
    ```jsx
    import { Switch } from 'rewind-uikit';

    <Switch id='mode' color='primary' />;
    ```

  </DocsTemplate>
  <div>
    | Prop     | Values                                  |
    | -------- | ---------------------------------------- |
    | color    | `primary`, `secondary`|
  </div>
</div>
