---
slug: checkbox
title: Checkbox
---

import React, { useState } from 'react'
import { Checkbox } from 'rewind-uikit'
import { DocsTemplate } from '../../src/components/DocsTemplate.tsx'

export const CheckboxPreview = () => {
  const [isChecked, setIsChecked] = useState(false);

const handleCheckboxChange = () => {
    setIsChecked(!isChecked);
  };

return (

<div>
  <Checkbox
    color='primary'
    checked={isChecked}
    onChange={handleCheckboxChange}
  />{' '}
  Check this option
  {isChecked ? <p>Option checked!</p> : <p>You don't check the option...</p>}
</div>
) }

Checkboxes allow users to select one or more options from a set.
They can also support an indeterminate state, making them useful for complex forms where multiple selections and hierarchies need to be managed.

<div className="flex flex-col gap-8 justify-center">
  <DocsTemplate preview={CheckboxPreview()}>

```js
import { Checkbox } from 'rewind-uikit'
import { useState } from 'react'

export const CustomComponent = () => {
  const [isChecked, setIsChecked] = useState(false)

  const handleCheckboxChange = () => {
    setIsChecked(!isChecked)
  }

  return (
    <div>
      <Checkbox
        color='primary'
        checked={isChecked}
        onChange={handleCheckboxChange}
      />{' '}
      Check this option
      {isChecked ? (
        <p>Option checked!</p>
      ) : (
        <p>You don't check the option...</p>
      )}
    </div>
  )
}
```

  </DocsTemplate>
  
  <div>
    | Prop    | Values |
    | -------- | ------- |
    | color    | `primary`, `secondary`, `info` |
  </div>
  
</div>
