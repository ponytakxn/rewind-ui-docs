---
slug: input
title: Input
---

import { Input } from 'rewind-uikit'
import { DocsTemplate } from '../../src/components/DocsTemplate.tsx'

export const InputPreview = () => {
  return <Input color='primary' placeholder='Email' />
}

Input fields are fundamental for collecting user data, supporting various types like text, password, email, and more.
They come with validation capabilities to ensure the data entered is correct and complete.

<div className="flex flex-col gap-8 justify-center">
  <DocsTemplate preview={InputPreview()}>
    
    ```js
    
    import { Input } from 'rewind-uikit'

    <Input color='primary' placeholder='Email' />
    ```

  </DocsTemplate>
</div>
