---
slug: badge
title: Badge
---

import { Badge } from 'rewind-uikit'
import { DocsTemplate } from '../../src/components/DocsTemplate.tsx'

export const BadgePreview = () => {
  return (
    <Badge size='sm' variant='solid' color='primary'>
      This is a badge
    </Badge>
  )
}

A Badge component is perfect for displaying small but important pieces of information, such as notification counts or status indicators.
It can be attached to other UI elements like icons or buttons to draw attention to new or unread items.

<div className="flex flex-col gap-8 justify-center">
  <DocsTemplate preview={BadgePreview()}>
    ```js
      import { Badge } from 'rewind-uikit'

      <Badge size='sm' variant='solid' color='secondary'>Texto</Badge>
    ```

  </DocsTemplate>
  <div>
    | Prop    | Values |
    | -------- | ------- |
    | size | `sm` , `md` |
    | variant | `solid` , `outline` |
    | color | `primary` , `secondary` , `info` |
  </div>
</div>
