---
slug: button
title: Button
---

import { Button } from 'rewind-uikit'
import { DocsTemplate } from '../../src/components/DocsTemplate.tsx'

export const ButtonPreview = () => {
  return (
    <Button
      size='md'
      color='primary'
      variant='solid'
      onClick={() => alert('button clicked!')}
    >
      Click me!
    </Button>
  )
}

Buttons are essential for user interactions, triggering actions or events like submitting forms, opening dialogs, or navigating to other pages.
Available in various styles, sizes, and types, they can be customized to fit any design requirement.

<div className="flex flex-col gap-8 justify-center">
  <DocsTemplate preview={ButtonPreview()}>
    ```js
    import { Button } from 'rewind-uikit'

    <Button
      size='md'
      color='primary'
      variant='solid'
      onClick={() => alert('button clicked!')}>Click me!
    </Button>
    ```

  </DocsTemplate>
  <div> 
    | Prop    | Values |
    | -------- | ------- |
    | size | `sm` , `md` , `lg` |
    | variant | `solid` , `outline` , `ghost` |
    | color | `primary` , `secondary` , `info` , `success` , `danger` |
  </div>
</div>
