---
slug: textarea
title: Text Area
---

import React from 'react'
import { TextArea } from 'rewind-uikit'
import { DocsTemplate } from '../../src/components/DocsTemplate.tsx'

export const TextAreaPreview = () => {
  return <TextArea />
}

Text Areas provide multi-line input fields for larger text entries, such as comments, messages, or descriptions.
They support features like resizing and validation, making them versatile for various applications.

<div className="flex flex-col gap-8 justify-center">
  <DocsTemplate preview={<TextAreaPreview />}>
    ```jsx
    import { TextArea } from 'rewind-uikit';

    <TextArea />;
    ```

  </DocsTemplate>
  <div>
    | Prop     | Values              |
    | -------- | ------------------- |

  </div>
</div>
