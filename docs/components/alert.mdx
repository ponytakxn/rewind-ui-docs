---
slug: alert
title: Alert
---

import { Alert, AlertHeader, AlertDescription } from 'rewind-uikit'
import { DocsTemplate } from '../../src/components/DocsTemplate.tsx'

export const WarningPreview = () => {
  return (
    <Alert type='warning'>
      <AlertHeader>Warning</AlertHeader>
      <AlertDescription>Cuidado con quien comparte sus datos</AlertDescription>
    </Alert>
  )
}

The Alert component delivers crucial feedback to users. It supports different variants such as success, danger, warning, and info messages, ensuring users are promptly informed about the status of their actions or any system updates.

<div className="flex flex-col gap-8 justify-center">
  <DocsTemplate preview={WarningPreview()}>
    ```js
    import { Alert, AlertHeader, AlertDescription } from 'rewind-uikit'

    <Alert type='warning'>
      <AlertHeader>Warning</AlertHeader>
      <AlertDescription>
        Cuidado con quien comparte sus datos
      </AlertDescription>
    </Alert>
    ```

  </DocsTemplate>
    <div> 
    | Prop    | Values |
    | -------- | ------- |
    | variant |  `success` , `info` , `warning` , `danger` |
  </div>
  
</div>
