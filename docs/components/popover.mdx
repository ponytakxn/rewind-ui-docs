---
slug: popover
title: Popover
---

import { Popover, PopoverTrigger, PopoverContent, Button } from 'rewind-uikit'
import { DocsTemplate } from '../../src/components/DocsTemplate.tsx'

export const PopoverPreview = () => {
  return (
    <Popover orientation='bottom'>
      <PopoverTrigger>
        <Button variant='outline' color='primary' size='sm'>
          Open Popover
        </Button>
      </PopoverTrigger>
      <PopoverContent>Content</PopoverContent>
    </Popover>
  )
}

Popovers are versatile overlays that display content when triggered by user actions, such as clicks or hovers.
They can contain any type of information, from simple text to complex interactive elements, providing contextual information without cluttering the main interface.

<div className="flex flex-col gap-8 justify-center">
  <DocsTemplate preview={<PopoverPreview />}>
    ```jsx
    import { Popover, PopoverTrigger, PopoverContent, Button } from 'rewind-uikit'

    <Popover orientation='bottom'>
      <PopoverTrigger>
        <Button variant='outline' color='primary' size='sm'>
          Open Popover
        </Button>
      </PopoverTrigger>
      <PopoverContent>Content</PopoverContent>
    </Popover>
    ```

   </DocsTemplate>
   <div> 
    | Prop    | Values |
    | -------- | ------- |
    | orientation | `top` , `bottom`,`left` , `right`  |
  </div>
</div>
